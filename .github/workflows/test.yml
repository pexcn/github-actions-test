name: Test

on: push

jobs:
  build:
    runs-on: ubuntu-20.04
    container:
      image: pexcn/docker-images:buster-openwrt
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Test
        run: df -h && pwd && whoami
